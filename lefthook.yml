pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,mjs,gs,svelte,css,json,html,yml,md}"
      run: npx oxfmt {staged_files}
    lint:
      glob: "*.{js,mjs}"
      run: npx oxlint --fix {staged_files}
